__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

.venv/
venv/
env/
# Throwaway spike environments; rebuild from the READMEs in spike/*/.
.venv-spike/
.venv-quant/

.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Simulation artifacts are reproducible from seeds; don't commit them.
runs/
*.sim.json

# Calibration session files are measurements, not artifacts -- but they are large and
# machine-specific. Commit the fitted tolerance_table.json (with its provenance notes),
# not the raw sessions.
sessions/session_*.json
sessions/smoke.json

# The IP-to-ASN table is 8.5 MiB, rebuilt daily upstream, and re-fetchable in seconds:
# tools/fetch_asn_table.py. Pinning a stale copy in git would be worse than downloading it.
data/

.DS_Store
.idea/
.vscode/
